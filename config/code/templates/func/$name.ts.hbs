export default function {{basename outpath}}() {}

if (cfgTest && cfgTest.url === import.meta.url) {
  const { assert, describe, test } = cfgTest

  describe("{{dirname outpath}}/{{basename outpath}}", () => {
    test.todo("Not implemented")
  })
}
